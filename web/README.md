# web

dash 控制台前端项目（占位）。

## 职责边界

- 前端单页应用（SPA），基于 TypeScript。
- 提供节点监控看板、历史曲线图表、三网测速面板、节点与凭据管理等界面。
- 构建产物将作为静态资源内嵌（embed）至 `dashd` 二进制中，实现单二进制交付。

## 对外接口

- 构建输出位于 `dist/`，由服务端路由挂载。

## 依赖谁

- 服务端 `/api/v1/**` REST API 及 SSE 事件流。
