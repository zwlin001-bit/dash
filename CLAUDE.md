# dash

个人 VPS 管理平台：监控 + 云资产（阿里云 / GCP）+ 代理服务器管理。

---

## 协作模式（新会话先读这段）

**用户只说「做什么」，其余全流程由我承担：**

```
用户提需求  →  我拆解成任务书  →  AGY（反重力）实施  →  我验收  →  我合并
```

| 角色 | 承担方 | 产出 |
|---|---|---|
| 设计 | **我（Claude）** | `docs/` 下的设计文档 + `docs/agy/tasks*/` 的任务书 |
| 实施 | AGY（反重力） | 代码，只在任务分支上 |
| 验收 | **我** | 验收记录写回任务书 |
| 合并 | **我** | 通过后合并回 `main` |

### 五条铁律

1. **文档只进 `main`，我自己 commit + push。代码只进任务分支 `agy/p1-NN-xxx`。**
   AGY 不提交 `docs/` 下任何文件。改文档用
   `git worktree add <scratchpad>/wtm main`（主工作区被 AGY 占用，别切分支），
   改完 commit + push + `git worktree remove`。
2. **每次输出必须带「任务清单 + 依赖关系」**，扁平列表即可：
   `08 Agent主循环  依赖 06 07`。不画依赖图、不摆多层表格。
   **不标依赖，用户会默认可并行，直接开多个 AGY 同时跑。**
3. **验收结论只有「✅ 通过 / ❌ 不通过」两种。** 任何一处要改就是不通过。
   所有需修改项一律**写回任务书**，编号 `F1` `F2`…，含复现方式和具体修法。
   已通过的项明确标注「不要再动」。修完追加下一轮，不覆盖上一轮。
4. **验收要真跑，不看代码猜。** 跑测试、跑基准、连真库、对照真机数据。
   历史上抓到的问题都是跑出来的：脱敏只覆盖字符串、lint 慢 115 秒、
   testdata 空目录 git 不跟踪、连接数读错文件。
5. **发现设计有问题：先改 `docs/`，再改代码。** 绝不代码先跑偏、文档后补。

### 常用命令

`up` 多机全分支同步 · `upst` 只看状态 · `uppush` 只推不提交
（定义在 `~/.bashrc`，增加服务器改 `DASH_HOSTS` 一行）

---

## 先读这个

**`PRINCIPLES.md` 是硬性约束，动手前必读。** 尤其：
- 数据库只写可移植 SQL（Oracle ADB → 未来 MySQL），禁止 Oracle 方言
- agent 有明确的资源预算（RSS ≤20MB、CPU ≤0.5%、二进制 ≤6MB），超标视为缺陷
- 改表结构 / 改协议，必须同步改文档

## 设计文档

`docs/` 是设计的事实来源，索引见 `docs/README.md`。
第一期任务清单：`docs/agy/tasks/README.md`。

## 当前进度

★ **第一期（监控信息 + 机器清单维护）21 个任务全部完成并合并进 `main`。**
每个任务的验收履历在 `docs/agy/tasks/` 对应文件末尾，
总览见 `docs/agy/tasks/README.md` 的「当前进度」。

端到端链路已跑通：agent 注册 → 上报 → 数据进 Oracle ADB → 界面可查 → 事件产生。
