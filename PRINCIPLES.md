# dash 工程原则

> 本文件是 **约束**，不是建议。设计（Claude）、实现（反重力）、验收（Claude）三方都以此为准。
> 任何与本文件冲突的实现，验收阶段一律打回；确实需要改原则的，先改本文件并说明理由。

---

## 0. 角色与流程

| 角色 | 承担方 | 产出 |
|---|---|---|
| 设计 | Claude | `docs/` 下的架构、数据库、协议、部署文档；接口契约 |
| 实现 | 反重力 | 代码 + 与代码同步更新的文档 |
| 验收 | Claude | 对照本文件与 `docs/` 逐条检查 |

**流程**：设计文档先行 → 实现 → 文档同步更新 → 验收。
实现过程中发现设计有问题，**先提出、改文档、再改代码**，不允许代码先跑偏、文档后补。

---

## P1. 文档与代码同步（硬性）

1. `docs/` 是唯一的设计事实来源。代码与文档不一致时，视为缺陷。
2. **任何改动数据库表结构的提交，必须同时更新 `docs/02-database.md` 和 `migrations/`。**
3. **任何改动 agent ↔ server 协议的提交，必须同时更新 `docs/04-protocol.md` 并升版本号。**
4. 每个模块的目录下放 `README.md`，说明它的职责边界、对外接口、依赖谁。
5. 重要决策写进 `docs/adr/NNNN-*.md`（背景 / 选项 / 决定 / 代价），不要只留结论。
6. 文档写中文，标识符、字段名、错误码写英文。

---

## P2. 数据库：只写可移植 SQL

当前后端是 **Oracle 自治数据库（ADB）**，后续要能平迁 **MySQL 8**。

**必须遵守：**

1. **业务代码不用任何 Oracle 方言**：禁止 `ROWNUM`、`CONNECT BY`、`NVL`、`DECODE`、`SYSDATE`、`FROM dual`、`MERGE`、`(+)` 外连接、`RETURNING INTO`、层次查询、Oracle 专有函数。
   （唯一例外：`internal/db/dialect` 包内的 upsert 模板允许用 `MERGE`，见第 9 条。）
2. **不用数据库端逻辑**：不用触发器、存储过程、函数、序列、物化视图、分区表、DB 定时任务。所有逻辑在应用层。
3. **主键由应用生成**：统一 ULID（26 位字符串），存为 `VARCHAR(26)`。不用 IDENTITY / AUTO_INCREMENT / SEQUENCE。
4. **时间统一存 epoch 毫秒整数**（列名一律 `*_at_ms` / `*_ms`）。不用 `DATE` / `TIMESTAMP` / 时区类型，不用 `SYSDATE` / `NOW()`——时间由应用生成。
5. **布尔存整数 0/1**，列名 `is_*` / `has_*`。
6. **空字符串等价于 NULL**（Oracle 语义）。可选字符串列一律可空，应用层把 `NULL` 当 `""` 处理，**绝不依赖 `'' <> NULL` 的区别**。
7. **标识符规则**：全小写、snake_case、不加引号、长度 ≤ 30 字符。避开保留字（见 `docs/02-database.md` 的黑名单）。
8. **DDL 可以分方言**（由同一份抽象 schema 生成），**DML 和查询必须一份通吃**。
9. **允许存在的方言差异只有三处**，且必须集中在 `internal/db/dialect` 一个包里：
   - upsert 语句
   - 分页语句（`FETCH FIRST` vs `LIMIT`）
   - 批量插入 / 占位符风格（`:1` vs `?`）
   出现第四处，说明设计错了，回来改设计。
10. **JSON 存文本列，在应用层解析**。不用 `JSON_VALUE` / `JSON_TABLE` / MySQL JSON 函数。
11. 每张表都要有明确的保留策略与清理方式；清理按时间区间分块 `DELETE`，不用 `LIMIT`。
12. 迁移脚本编号递增、只增不改，记录在 `schema_migrations` 表；每个编号提供 oracle 与 mysql 两份 DDL。

---

## P3. Agent：通用、轻、可解释

1. **单个静态二进制，`CGO_ENABLED=0`**。同一份产物在 Alpine(musl) / Debian / Ubuntu 上直接跑，不依赖 glibc、不依赖任何运行时。
2. 支持架构：`linux/amd64`、`linux/arm64`、`linux/armv7`。
3. **资源预算是验收指标，不是愿望**：
   - 常驻 RSS ≤ 20 MB（目标 12 MB）
   - 稳态 CPU ≤ 0.5% （单核，5 秒采集间隔）
   - 稳态磁盘写入 ≤ 1 次/分钟，且不写自己的日志文件
   超标即验收不通过。
4. **采集分级**，不同指标不同频率（详见 `docs/03-agent.md`）。昂贵的采集（连接数、进程数、磁盘用量）绝不跟 CPU/内存同频。
5. **不带本地数据库**，不落盘缓存历史数据。状态文件只有一个，且最多每分钟原子写一次。
6. 日志写 stderr，交给 systemd / OpenRC 收，agent 自己不做日志轮转。
7. **默认以非 root 运行**。需要特权的能力单独拆出，明确声明。
8. **默认关闭远程执行**。开启后默认只允许服务端声明的动作清单（action catalog），任意 shell 是需要逐台显式开启的第二档。
9. **不自更新**：不从 GitHub 拉二进制。升级由服务端下发（服务端自己托管产物 + sha256 校验）。
10. 一条命令装完，同时支持 systemd 和 OpenRC，自动识别。

---

## P4. 架构：按边界拆，不按技术拆

1. 三个边界上下文互不直接调用，只通过明确契约交互：
   - **监控**（agent、指标、告警）
   - **资产与云**（VPS 清单、阿里云、GCP）
   - **代理管理**（代理节点的创建、部署、配置）
2. 云厂商 / 资源类型走 **Provider 插件契约**。**新增一个云厂商 = 新增一个 provider，不改核心代码。**
3. Provider 可以用任意语言实现（**包括 Python**），以独立进程运行，通过 Unix socket 上的 JSON-RPC 与主进程通信。Go 与 Python 混合是被明确允许的，边界是 RPC 契约而不是共享代码。
4. 所有耗时/易失败的操作（建机器、装代理、同步云资源）走**统一的 Job 引擎**：可持久化、可重试、可恢复、有审计。不允许在 HTTP 请求里同步干这些事。
5. 服务端资源可以吃得多一点，换取简单和可靠——但客户端不行。

---

## P5. 部署：一条命令

1. `./setup.sh install` 完成全部安装部署。
2. **交互只问一个必填项：域名。** 其余全部有合理默认值。
3. 默认监听 **443**，证书自动申请与续期。
4. **域名写入数据库**，可在界面里维护修改，改完自动重新签发证书。配置文件里只留启动自举必需的东西（数据库连接、主密钥路径）。
5. `setup.sh` 提供 `install / upgrade / uninstall / status` 子命令，可重复执行且幂等。
6. 服务以非 root 用户运行，通过 `CAP_NET_BIND_SERVICE` 绑定 443。

---

## P6. 安全

1. 凭据（云厂商 AK/SK、SSH 私钥、agent token）加密存储，信封加密，主密钥不进数据库。
2. agent 注册用一次性 enrollment token 换取长期 token；长期 token 可单机吊销。
3. 所有变更类操作写审计日志：谁、何时、对哪个资源、做了什么、结果。
4. 远程执行的输入永远不拼接进 shell 字符串。

---

## P7. 可验收

每个模块交付时必须能回答：
- 怎么本地跑起来？
- 怎么验证它是对的（测试 / 手工步骤）？
- 它的失败模式是什么，怎么观察到？

做不到就不算完成。
