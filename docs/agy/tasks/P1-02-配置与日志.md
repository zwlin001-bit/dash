# P1-02 配置与日志

> 第一期 · 任务 02/19 ｜ 前置：01 ｜ 分支：`agy/p1-02-config-log`

## 目标

统一的配置读取和日志输出。**所有机密都要能被自动打码。**

## 交付物

```
internal/config/    读 /etc/dash/config.toml，支持 DASH_* 环境变量覆盖
internal/logx/      结构化日志 + redact 打码函数
deploy/config.example.toml   已存在，按实现校准
```

配置项参考 `deploy/config.example.toml`：`[db]` 段（driver/user/password/dsn/wallet_path/连接池，
其中 **`wallet_path` 当前留空**——ADB 已关闭 mTLS，用连接串直连）、
`[server]` 段（listen/listen_acme/data_dir）。

## 接口签名（照这个写）

```go
package config

type Config struct {
    DB     DBConfig     `toml:"db"`
    Server ServerConfig `toml:"server"`
}

type DBConfig struct {
    Driver          string `toml:"driver"`            // "oracle" | "mysql"
    User            string `toml:"user"`
    Password        string `toml:"password"`          // 支持 ${DASH_DB_PASSWORD} 占位
    DSN             string `toml:"dsn"`
    WalletPath      string `toml:"wallet_path"`
    MaxOpenConns    int    `toml:"max_open_conns"`    // 默认 20
    MaxIdleConns    int    `toml:"max_idle_conns"`    // 默认 10
    ConnMaxLifetimeS int   `toml:"conn_max_lifetime_s"` // 默认 1800
}

type ServerConfig struct {
    Listen     string `toml:"listen"`      // 默认 ":443"
    ListenACME string `toml:"listen_acme"` // 默认 ":80"
    DataDir    string `toml:"data_dir"`    // 默认 "/var/lib/dash"
    MasterKey  string `toml:"master_key"`  // 默认 "/etc/dash/master.key"
}

func Load(path string) (*Config, error)   // 文件 → 环境变量覆盖 → 校验 → 填默认值
func (c *Config) Validate() error
func (c *Config) Redacted() *Config       // 深拷贝并把机密替换成 "***"，用于日志
```

```go
package logx

func Init(level string, format string)    // format: "json" | "text"
func Debug(msg string, kv ...any)
func Info(msg string, kv ...any)
func Warn(msg string, kv ...any)
func Error(msg string, kv ...any)
func With(kv ...any) *Logger              // 带上下文的子 logger

// Redact 是全工程唯一允许输出可能含机密内容的途径
func Redact(s string) string
func RedactDSN(dsn string) string         // 专门处理 DSN 里内嵌的密码段
```

底层用标准库 `log/slog`，**不引入第三方日志库**（P3 依赖纪律）。

### 必须打码的内容

| 来源 | 规则 |
|---|---|
| `DBConfig.Password` | 整体 `***` |
| `DSN` | 匹配 `password=…` / `//user:pw@` 形式，只替换密码段，保留其余便于排查 |
| wallet 密码 | 整体 `***` |
| agent token / session token / enrollment token | 只保留后 4 位：`***abcd` |
| 主密钥内容 | 永远不输出，连打码后的都不输出 |

**环境变量覆盖规则**：`DASH_` + 大写路径，`.` 换成 `_`。
例：`DASH_DB_PASSWORD`、`DASH_SERVER_LISTEN`。

### 配置项与设置项的边界

★ **能放数据库的都放数据库。** `config.toml` 只放「不连数据库就没法启动」的东西。
域名、保留期、采集间隔全在 `settings` 表（`10-schema-spec.md` §1、§5），
**不要往 `config.toml` 里加它们**。

## 约束

- **域名不在配置文件里**，在数据库 `settings` 表的 `site.domain`（P5.4）。
  配置文件只放「不连数据库就没法启动」的自举项
- 优先级：命令行 > 环境变量 > 配置文件
- 日志写 **stderr**，结构化（JSON 或 key=value），**不自己做文件轮转**——交给 systemd/OpenRC
- **`logx` 必须提供 redact 函数，并且是唯一允许输出配置内容的途径。**
  密码、token、wallet 密码、DSN 里的密码段，一律打码成 `***`
- 数据库密码支持从环境变量 `DASH_DB_PASSWORD` 读，避免落在文件里

## 验收

1. 配置文件 + 环境变量 + 命令行三种来源的优先级正确（写单元测试覆盖）
2. **把一个带密码的完整配置打进日志，输出里翻不到明文密码**——这条会实际去 grep 日志
3. DSN 里内嵌密码的情况也要被打码
4. 配置文件缺失时给出可读的错误信息，不是 panic

## 边界

不做主密钥/信封加密（第一期不存云厂商凭据）。不做 ACME、不做数据库连接。

---

# 验收记录

## 第 1 轮 · 2026-09-07 · ❌ 未通过（1 项必修）

分支 `agy/p1-02-config-log`，提交 `16df8f8`。

### ✅ 已通过 —— 不要再动这些

| 验收项 | 实测结果 |
|---|---|
| 三来源优先级（文件 < 环境变量 < 命令行） | ✅ 实测 `user=from-cli`（CLI 覆盖 env 覆盖 file）、`driver=mysql`（env 覆盖 file）、`listen=:8443`（file）、`max_open_conns=20`（默认值兜底） |
| 配置文件缺失的错误 | ✅ `config file not found: /nonexistent/x.toml: file does not exist`，包装了 `os.ErrNotExist`，未 panic |
| 坏 TOML 的错误 | ✅ `failed to parse toml config bad.toml: toml: expected character ]`，可读 |
| `${VAR}` 占位展开 | ✅ 有测试覆盖 |
| `deploy/config.example.toml` 可解析 | ✅ 有测试覆盖 |
| **DSN 内嵌密码打码** | ✅ `oracle://admin:SECRET@host` → `oracle://admin:***@host`；`RedactDSN` 覆盖 URI / EZConnect / TNS `(PASSWORD=)` 三种形式 |
| 域名不在配置文件里 | ✅ `Config` 里没有域名字段 |
| 未引入第三方日志库 | ✅ 底层是 `log/slog`；`go.mod` 只多了 `go-toml/v2`（标准库无 TOML，理由成立） |
| `make lint test` | ✅ 全过，18 个用例 |

**架构选择是对的，不要推翻**：把脱敏做成 `slog.Handler`（`redactHandler`）自动生效，
而不是靠调用方记得手动调 `Redact()`——这比任务书要求的更稳。**问题是它做得不完整，不是方向错了。**

### ❌ F1 必须修 —— 脱敏只覆盖字符串，其余类型全部裸奔

`sanitizeAttr` 的 `switch a.Value.Kind()` 只处理 `KindString` 和 `KindGroup`，
`default: return a` 让所有其他类型原样透传。

实测七种形态，**六种泄露**：

```
logx.Info("cfg",    "cfg",  cfg)                    → ❌ password 明文
logx.Info("ptr",    "cred", &c)                     → ❌ 明文
logx.Info("map",    "m",    map[string]string{...}) → ❌ 明文
logx.Info("slice",  "s",    []string{"password=X"}) → ❌ 明文
logx.Info("error",  "err",  err)                    → ❌ 明文
logx.Info("nested", "outer", struct{Inner Cred}{})  → ❌ 明文
logx.Info("dsn",    "dsn",  "oracle://u:p@h")       → ✅ 已打码（唯一过的）
```

**这直接不满足任务书的验收第 2 条**（「把一个带密码的完整配置打进日志，
输出里翻不到明文密码——这条会实际去 grep 日志」）。实测：

```
logx.Info("loaded config", "cfg", cfg)
→ {"msg":"loaded config","cfg":{"db":{...,"password":"GaPvWXBd5wsPj2n",...}}}
```

★ **其中 `error` 那条最要命。** 最常见的真实路径就是
`logx.Error("db connect failed", "err", err)`，而 go-ora 的连接错误里**会带上完整 DSN**。
这个洞不补，第一次连库失败就会把密码写进 journald。

**修法**：在 `sanitizeAttr` 里补 `KindAny` 分支，复用 `redact.go` 里已有的
`sanitizeValue` 反射机制做深度清洗。

```go
case slog.KindAny:
    v := a.Value.Any()
    if e, ok := v.(error); ok {
        return slog.String(a.Key, Redact(e.Error()))
    }
    if s, ok := v.(fmt.Stringer); ok {
        return slog.String(a.Key, Redact(s.String()))
    }
    return slog.Any(a.Key, sanitizeAny(v))   // 反射深度清洗
```

`sanitizeAny` 的规则：
- **struct / pointer**：逐字段。字段名命中 `isSensitiveKey` → `***`；
  命中 `isTokenKey` → `RedactToken`；字符串字段 → `Redact`；嵌套结构递归
- **map**：key 命中敏感词 → 打码 value；否则递归 value
- **slice / array**：逐元素递归
- **string**：`Redact`
- ★ **必须有深度上限（建议 8 层）和循环引用保护**，
  否则一个自引用结构会让日志调用直接栈溢出——日志把进程搞崩比泄露还糟

**复验必须覆盖这七种形态**，直接把上面那段测试代码加进 `log_test.go`：
struct 值、struct 指针、map、slice、error、嵌套 struct、`map[string]any` 里的 dsn。
**逐个 grep 断言明文不出现。**

### ⚠️ 顺手改

| # | 问题 |
|---|---|
| F2 | `os.Expand` 会连 `$VAR`（无花括号）一起展开。DSN 里若含 `$` 会被误吞。改成只展开 `${VAR}` 形式，或对未定义变量保留原样 |
| F3 | PR 说明里补上新增依赖 `go-toml/v2` 的理由（`03-rules.md` §2 / §7 要求） |

### 复验方式

```sh
make lint test

# F1 复验：把这七种形态打进日志，逐个 grep 明文
cat > /tmp/redact_check.go <<'GO'
// struct 值 / struct 指针 / map / slice / error / 嵌套 struct / map[string]any 里的 dsn
GO
# 断言：输出里不允许出现任何明文密码或 token
```

---

## 第 2 轮 · 待验收

## 第 2 轮 · 2026-09-07 · ✅ 通过

提交 `1e4512d`。

| 项 | 实测 |
|---|---|
| F1 全类型脱敏 | ✅ 七种形态**全部打码**：struct 值 / struct 指针 / map / slice / error / 嵌套 struct / `map[string]any` 里的 dsn |
| F1 深层嵌套 | ✅ `map[string]any{"a":map[string]any{"b":[]any{Cred{...}}}}` 三层深处的密码也被打码 |
| F1 Token 保留后 4 位 | ✅ `tok_SECRET_TOKEN99` → `***EN99` |
| F1 循环引用保护 | ✅ 自引用结构输出 `"[circular reference]"`，**未栈溢出** |
| F2 `${VAR}` 精准展开 | ✅ `host$PLAIN/svc?p=${BRACED}` → `host$PLAIN/svc?p=YY`，无花括号的 `$PLAIN` 保持原样 |
| 回归 | ✅ `go test ./...` 全过 |

**任务 02 关闭。已合并到 main：`62e9122`。**
