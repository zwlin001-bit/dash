# P2-09 云账单聚合与预算告警

> 第二期 · 任务 09 ｜ 前置：**P2-02 ✅ P2-03 ✅ P2-08**（预算告警要复用告警引擎）
> ｜ 分支：`agy/p2-09-cloud-billing`

> ★ **完成后必须自己 `git push -u origin <分支名>`**，并确认
> `git log --oneline -1 origin/<分支名>` 能看到你的提交。只提交到本地等于没交付。

## 目标

**把云账单拉回来、留历史、按维度汇总、超预算告警。**

★ **设计在 `docs/15-cloud-billing.md`（本任务新增的设计文档），照它实现。**
发现设计有问题：先改 `docs/15-cloud-billing.md`，再改代码。

## 与现有东西的关系

★ 先分清三样容易混的东西，**不要互相覆盖**：

| | 是什么 | 谁维护 |
|---|---|---|
| `node_billing` | **人工录入**的 VPS 月费与到期日 | 用户在界面上填 |
| `cloud_resources.billing_json` | 资源同步时带回的**价格快照** | P2-02 的同步 |
| `bill_periods` / `bill_items` | ★ **从云厂商 API 拉回的真实账单** | 本任务 |

P2-02 已经能查单实例当月账单，但那只是一个数字，答不了「这个月一共花了多少、
钱花在哪、比上月多还是少、快超预算了吗」。

## 交付物

```
docs/15-cloud-billing.md                       （已随本任务一起交付，实现时对照）
migrations/0008_billing.{mysql,oracle}.sql     bill_periods / bill_items / bill_budgets
internal/billing/                              同步、汇总、预算评估
internal/provider/aliyun/                      新增 bill.list 实现
internal/api/billing/                          查询与预算 CRUD API
web/src/pages/Billing/                         账单页
```

## 约束

### 表与字段

照 `15-cloud-billing.md` §2。两处容易做错的：

- ★ **`period` 用 `"2026-09"` 字符串**，不是时间戳。跨时区没歧义、可比可排序
- ★ **`cloud_resource_id` 必须可空。** 账单里大量条目不对应任何实例
  （公网带宽包、快照、OSS）。**不要为了外键完整性把它们丢掉** ——
  那正是「钱花哪儿了」最容易失控的部分

### 币种：原币存储，禁止自动折算

★ 跨账号汇总时**同币种相加，不同币种分组显示**，不许偷偷折算成人民币。
汇率会变，折算过的历史数字既不可复现也不可对账。
界面上并排列「CNY 1234.56 / USD 78.90」即可。

### 同步

- 走 Job 引擎（P2-03），job kind = `bill.sync`
- ★ **当月账单是变动的**，每次同步覆盖当月；
  **已关账的历史月份只拉一次**（`sync_state='ok'` 且 `period` < 当月即跳过）
- 默认回补最近 12 个月，可配
- ★ **账单 API 的限流比 ECS 严得多。** 回补 12 个月要**串行 + 退避**，
  不许并发轰。某个账号失败写 `error_text`，**不阻塞其他账号**

阿里云用 `QueryBillOverview`（月度总览）+ `QueryInstanceBill`（明细，**要分页**），
endpoint 沿用 `cloud_accounts.account_site`。

### Provider 契约加 `bill.list`

按 `15-cloud-billing.md` §5 的形状实现，并 **同步写进 `01-architecture.md` §3.3 的方法表**
（`CLAUDE.md` 要求改协议必须同步改文档）。

★ 新增 provider 只要实现这个方法就能接入账单，**不改 dashd**。

### 预算告警：复用告警引擎，不新造

★ **不要写第二套告警状态机。** 给 P2-08 的 `alert_rules` 加一个 `rule_kind = 'budget'`：

| rule_kind | threshold 含义 | 数据来源 |
|---|---|---|
| `budget` | 当月用量 ÷ 预算 的比例（0.8 = 80%） | `bill_periods.total_amount` ÷ `bill_budgets.amount` |

每小时评估一次，去抖 / 静默 / 恢复通知全部沿用既有状态机。

事件类型按 `09-events-notify.md` §2.1 登记：

| event_type | 默认级别 | 默认处置 |
|---|---|---|
| `billing.budget_warning` | warning | store+notify |
| `billing.budget_exceeded` | critical | store+notify |
| `billing.sync_failed` | error | store+notify |

### 界面

- 顶部：当月合计（**按币种分组**）、环比上月、预算进度条
- 趋势：最近 12 个月柱状图，★ **按币种分开画**，不要叠在一起
- 明细：按资源类型 / 单资源 / 标签三种维度切换汇总
- ★ **「未关联资源」单列一组** —— 忘记删的公网 IP、快照都藏在这里
- 缺数据显示 `--`，不用 `0` 顶替
- 颜色一律走 token（P2-05 的 `lint-css-tokens` 会拦）

## 验收

1. 配一个真实阿里云账号 → 同步 → **当月合计与阿里云控制台「费用中心」对得上**
2. 回补 12 个月 → 历史月份数据落库；★ **再同步一次，已关账月份不重复拉**
   （日志或 `synced_at_ms` 能证明）
3. ★ 账单里的公网带宽包 / 快照等条目 → **落进 `bill_items` 且 `cloud_resource_id` 为空**，
   界面上在「未关联资源」组里能看到，没被丢弃
4. 两个不同币种的账号 → 合计处**分组显示**，没有被折算成同一种币
5. 预算设 100 元、当月已用 85 元 → 收到 `billing.budget_warning`；
   超过 100 → 收到 `billing.budget_exceeded`
6. ★ 连续几小时都超预算 → **按 `silence_s` 去重**，不是每小时推一次
7. 把 BSS 权限去掉 → `billing.sync_failed` 推出来，`error_text` 里**无 AK/SK 明文**，
   且**其他账号的同步不受影响**
8. 按标签汇总：给两台机器打 `proxy` 标签 → 汇总金额 = 这两台的账单之和
9. `make lint`（含 SQL 方言与 CSS token）、`make test` 全绿
10. `01-architecture.md` §3.3 的方法表里有 `bill.list`

## 边界

- 只做阿里云。GCP / AWS / Oracle 的 `bill.list` 后续按同一契约补
- 不做成本优化建议、不做资源闲置分析
- 不做汇率折算（见上，这是刻意的）

---

# 验收记录

## 第 1 轮 · 2026-09-08 · ✅ 通过（合并成本很高）

分支 `agy/p2-09-cloud-billing`，提交 `9ab6dab`。

| 验收项 | 实测 |
|---|---|
| 表设计 | ✅ `period VARCHAR(7)` 字符串；★ **`cloud_resource_id` 可空**，带宽包/快照这类条目不会被丢 |
| 2 已关账月份只拉一次 | ✅ `sync.go:123` 有显式判断与日志 |
| 4 不做汇率折算 | ✅ 全仓库 grep `exchange` / `汇率` / `convert.*currency` **0 命中**，符合设计的刻意取舍 |
| 5/6 预算告警 | ✅ ★ **真的复用了告警引擎**：`internal/alert/types.go:9` `RuleKindBudget`，`state.go:297` 发 `billing.budget_exceeded` / `budget_warning`。没有第二套状态机 |
| 7 失败隔离与脱敏 | ✅ `sync.go:146` `logx.Redact(err.Error())` 后才写 `error_text`；按账号循环 + `continue`，单账号失败不影响其他 |
| 10 `bill.list` 进契约文档 | ✅ `01-architecture.md:113` |
| Go 测试 | ✅ 合并后 39 个包全绿 |

### 待补跑

`internal/billing` 的 5 个测试全部 SKIP（缺 MySQL），覆盖回补与关账跳过、
未关联资源、多币种、按标签汇总、预算告警与静默、BSS 失败隔离。见 P2-12。

真账号验收（1 与控制台对账、3 带宽包条目、8 按标签汇总）需部署后用真实凭据做。

### 次要 · 限流退避偏弱

任务书 §同步 要求「串行 + 退避」，实现是账期之间 `time.Sleep(100ms)` 固定间隔（`sync.go:140`），
串行有了，**退避没有**。首次回补 12 个月若撞上限流会直接失败而不是退避重试。
量不大，**先记着**，等真实回补跑一次看是否需要加。

### F1 · 与 P2-10 的合并冲突面很大（合并时已解）

P2-09 从 P2-08 拉分支，而 P2-10 同期在给 provider 契约加 `metric.list`，
两边同时改 `provider/client.go`、`provider/types.go`、`aliyun/client.go`、
provider 的 RPC 分发和一堆 mock ——**24 个文件冲突**。

裁决一律是「两个方法都要」，但机械并集会把函数体从中间切断
（`ListMetrics` 的收尾被 `ListBills` 的函数头吃掉），
有 7 处改用「从两侧各取完整函数再拼」重建，另补回被并集吃掉的 `"sort"` import。

★ **教训**：两个任务并行改同一个契约接口时，机械并集不安全。
下次排任务应当让「扩展 provider 契约」这件事串行，或者至少在任务书里
指定由谁先落地骨架。

**任务 P2-09 通过。**
