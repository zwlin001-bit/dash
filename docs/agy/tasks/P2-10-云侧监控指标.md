# P2-10 云侧监控指标接入

> 第二期 · 任务 10 ｜ 前置：P2-02 ✅ P2-03 ✅ P2-04 ✅ ｜ 分支：`agy/p2-10-cloud-metrics`

> ★ **完成后必须自己 `git push -u origin <分支名>`**，并确认
> `git log --oneline -1 origin/<分支名>` 能看到你的提交。只提交到本地等于没交付。

## 目标

**补上 agent 覆盖不到的那部分监控。**

★ **设计在 `docs/16-cloud-metrics.md`（随本任务一起交付），照它实现。**
发现设计有问题：先改文档，再改代码。

## 三个 agent 拿不到的场景

1. ★ **被保活停掉的实例**（P2-04）—— 机器关着 agent 就不在跑，
   界面上只能显示 `--`。但你恰恰想知道它停之前用了多少
2. **装不上 agent 的资源** —— RDS、SLB、OSS、公网带宽包
3. ★ **账号级 CDT 流量没有历史** —— P2-04 每轮查一次当前值用于决策，**但不落库**。
   于是看不到「这个月烧得多快」，而这正是保活阈值该定多少的依据

★ **这不是要取代 agent。** agent 是 5 秒粒度、指标全、免费；
云监控 1 分钟起步、指标少、调用要计费。**能用 agent 的一律用 agent。**

## 交付物

```
docs/16-cloud-metrics.md                       （已交付，实现时对照）
migrations/0009_cloud_metrics.{mysql,oracle}.sql   cloud_samples
internal/cloudmetric/                          拉取、落库、查询
internal/provider/aliyun/                      新增 metric.list 实现
web/src/pages/Cloud/ 与 CloudGuard/            曲线展示
```

## 约束

### 表

```
cloud_samples(cloud_account_id, res_ref str(191), metric_code str(40),
              ts_ms ts, value f64,
              PK(cloud_account_id, res_ref, metric_code, ts_ms))
  ix_cloud_samples_ts (ts_ms)
```

- ★ **用 `res_ref`（云厂商侧 id）而不是 `cloud_resource_id`**。
  资源在本地被删了，历史数据不该跟着消失。账号级指标用 `_account` 作 `res_ref`
- ★ **不做多级 rollup。** 云监控本来就 1 分钟起步、数据量比 agent 小两个数量级，
  再分层是过度设计。原始粒度 5 分钟，保留 90 天，走已有的分块清理

### 指标命名：不许另起一套

★ `metric_code` **沿用 `08-field-map.md` 里已有的名字**
（`cpu_pct` / `mem_used` / `net_up_bps` / `net_down_bps` / `traffic_month_up` …）。

同一个 CPU 使用率在 agent 侧和云侧叫不同名字，图表和告警规则就得写两遍。
**provider 负责把厂商的名字翻译成我们的**，这是 provider 该干的活。

### Provider 契约加 `metric.list`

按 `16-cloud-metrics.md` §3 的形状实现，并**同步写进 `01-architecture.md` §3.3 的方法表**
（`CLAUDE.md` 要求改协议必须同步改文档）。

### 拉取策略

- 走 Job 引擎（P2-03），job kind = `cloud.metric.sync`，默认 10 分钟一轮
- ★ **只拉「有人看」的**：默认只拉**被保活规则管着的实例** + **账号级 CDT 流量**。
  全量拉是要花钱的，且大部分资源根本没人看。要拉更多由用户显式勾选
- 增量：记 `(res_ref, metric_code)` 的 `last_at_ms`，只拉之后的点
- ★ 云监控 API 有限流，**串行 + 退避**，某个资源失败**不阻塞其他资源**

### 界面

- 资源详情页：云侧曲线与 agent 曲线**并排但明确区分来源**，
  ★ 标注「云监控 · 5 分钟粒度」。**不要混进同一条线**让人分不清哪个数是哪来的
- ★ **账号页加 CDT 流量月度曲线 + 阈值横线** —— 这是本任务最实用的一张图，
  直接回答「这个月烧得多快、离阈值还有多远」
- 缺数据显示 `--`，断点断线不连成直线
- 颜色走 token（`lint-css-tokens` 会拦）

## 验收

1. ★ **停掉一台被保活管着的实例** → 等一轮同步 → 资源详情页上
   **仍能看到它停机前的 CPU/流量曲线**，不是一片 `--`
2. ★ **账号页的 CDT 流量月度曲线有数据**，且与 P2-04 守卫页显示的当前用量对得上
3. 曲线上明确标着数据来源与粒度，agent 曲线与云侧曲线不混线
4. 跑一轮同步 → 再跑一轮 → ★ **第二轮只拉增量**（调用次数或日志能证明）
5. 未被保活规则覆盖的实例 → **默认不拉**，云监控调用次数不随资源总数线性增长
6. 把云监控权限去掉 → 拉取失败写事件，★ **保活与账单同步不受影响**
7. `metric_code` 与 `08-field-map.md` 一致 —— grep 对照，没有云侧专有的新名字
8. 90 天前的数据被清理任务删除
9. `01-architecture.md` §3.3 的方法表里有 `metric.list`
10. `make lint`（含 SQL 方言与 CSS token）、`make test` 全绿

---

## ★ 追加要求（2026-09-08，任务进行中补充）

> ⚠️ **本节是任务开工后追加的，实施者请确认已读到。**

### 界面照 aliyun-guard 的仪表盘来做

用户指定参考 [`Felix666-ship-It/aliyun-guard`](https://github.com/Felix666-ship-It/aliyun-guard)
的 web 面板（样式已确认可以照抄，见 P1-28）。我把它的结构拆解如下，**照这个骨架实现**：

**① 顶部指标带**（`.stat-band`）
4 列等宽卡片，每张：图标 + 小号灰色标签 + **大号 `tabular-nums` 数值**。
用来放：账号本月 CDT 用量 / 阈值余量 / 在线实例数 / 本月账单。

**② 实例卡片网格**（`.instances` 两列 → 窄屏单列）

每张卡片自上而下：

| 区块 | 内容 |
|---|---|
| `.card-head` | 实例名 + 次要信息（region / 规格）+ **状态徽章** + 右上角操作菜单 |
| `.metric-grid` | **5 列**小指标格（CPU / 内存 / 磁盘 / 上行 / 下行），每格「小标签 + 值」 |
| `.traffic-row` + `.progress` | 「已用 / 上限」一行，下面一条 pill 圆角进度条 |
| `.sparkline` | ★ **内嵌 SVG 曲线**，见下 |
| `.next-line` | 下一次日程事件时间 |
| `.result-line` | 上一轮评估结果 |

**③ ★ sparkline 曲线是这套界面的精华，五个细节都要做**

1. **面积渐变填充 + 圆角折线**（`.chart-area` / `.chart-line`）
2. ★ **虚线基准线**（`.chart-baseline`，`stroke-dasharray: 3 5`）——
   **这就是流量阈值线**，一眼看出离阈值还有多远
3. **当前点脉冲动画**（`.chart-point`，2.6s 循环）
4. ★ **hover 十字定位 + 悬浮 tooltip**（`.chart-focus` / `.chart-tooltip`），
   tooltip 内部用 `60px + 1fr` 两列网格排「标签 / 值」
5. **空数据态**（`.chart-empty`）—— 没数据时给一句说明，不是画一条平地线

**④ 骨架屏**（`.sk-card`）
加载中显示占位卡片，不要整页空白再突然跳出来。

### 落到我们仓库的三条硬约束

- ★ **颜色一律走 token**，`lint-css-tokens`（P2-05）会拦。
  参考实现里的 `--brand` / `--ok` / `--chart` 对应我们的
  `--accent` / `--ok` / `--color-teal`
- ★ **不引 Web Font**（`13-ui-spec.md` §2.6）。参考实现用的 Inter 不要引
- ★ **不引图表库**。这套 sparkline 是手写 SVG，几十行而已；
  项目里已有的 echarts 留给节点详情页的多跨度大图，卡片里的小曲线用手写 SVG

### 追加验收

11. 卡片上的 sparkline 具备：面积填充、**虚线阈值基准线**、当前点脉冲、
    hover tooltip、空数据态，五项齐全
12. 顶部 4 格指标带数值用等宽数字，浅色/深色两个主题下都不穿帮
13. 加载中显示骨架卡片，不是空白
14. 窄屏（≤ 768px）下实例卡片降为单列，指标格不挤成一团

## 边界

- ★ **不接告警**（`16-cloud-metrics.md` §6）。先把数据攒起来、图能看，
  再谈云侧指标的告警规则怎么写
- 只做阿里云。其他 provider 按同一契约后补
- 不做与 agent 数据的自动对账/校验

---

# 验收记录

## 第 1 轮 · 2026-09-08 · ✅ 通过（合并时补 1 处）

分支 `agy/p2-10-cloud-metrics`，提交 `b50b270`。

| 验收项 | 实测 |
|---|---|
| 表设计 | ✅ `cloud_samples` 完全照设计：**用 `res_ref` 而非 `cloud_resource_id`**，主键四元组正确 |
| 5 只拉「有人看」的 | ✅ `syncer.go:53` join `guard_rules` 且 `g.is_enabled=1 AND a.is_enabled=1 AND r.is_deleted=0` |
| 8 90 天保留 | ✅ 注册进 `internal/ingest/retention.go:35`，`retention.cloud_metrics_days` 默认 90，**复用已有的分块清理**而不是另写一套 |
| 增量拉取 | ✅ `store.go:48` `GetLastTs` 按 `(账号, res_ref, metric_code)` 取 `MAX(ts_ms)` |
| 不引图表库 / Web Font | ✅ grep 均 0 命中 |
| 追加11 sparkline 五要素 | ✅ 独立 `components/Sparkline/`，`strokeDasharray`(基准线) / 面积渐变 / tooltip / 空态 / 脉冲动画**全都有** |
| 追加14 窄屏 | ✅ `@media` 三档（900 / 768 / 480px） |
| lint | ✅ SQL 方言、CSS token 都过 |

### F1 · `metric.list` 没写进 `01-architecture.md`（合并时已补）

任务书验收项 9 明确要求「同步写进 §3.3 的方法表」，交付时漏了。
合并时已补上一行，现在方法表里有 `metric.list`。

### F2 · 与 P2-07 的合并冲突里有一处会踩坑（已裁决）

`CloudGuard.tsx` 里 P2-10 那侧写的是 `cyclesData.items`，
但 P2-07 的 `guard.ts` 已经用 `toItems` 拆包后再包成 `{cycles}`。
★ **按 P2-10 那侧合会渲染不出任何周期记录** —— 又是一次信封层级搞错。
合并时取了 HEAD 侧的 `cyclesData.cycles`。

演练弹窗也取了 HEAD 侧：P2-10 那侧把「拟关机/拟开机/保持不变」的分项和
动作明细表换成了四个聚合数字，丢了 P2-04 验收4 要求的「本轮会做什么」清单。

### 待补跑

`internal/cloudmetric` 的 5 个测试全部 SKIP（缺 MySQL），
覆盖存取、目标选择与增量、失败隔离、保留期清理、handler。见 P2-12。

浏览器实跑项（1 停机实例仍有曲线、2 CDT 月度曲线、3 双来源不混线、
12 两主题、13 骨架屏）需在部署后确认。

**任务 P2-10 通过。**
