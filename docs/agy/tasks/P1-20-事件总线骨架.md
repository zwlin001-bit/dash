# P1-20 事件总线骨架

> 第一期 · 任务 20 ｜ 前置：04 ｜ 分支：`agy/p1-20-events`
> 设计依据：[`../../09-events-notify.md`](../../09-events-notify.md)（**只做第 7 节说的那一薄层**）
> 迁移编号：**0002**

> ★ **完成后必须自己 `git push -u origin <分支名>`**，并确认
> `git log --oneline -1 origin/<分支名>` 能看到你的提交。只提交到本地等于没交付。

## 目标

**把「发事件」这个口子第一期就埋好**，否则第二期做通知时，
第一期所有模块都要回头改一遍。

第一期只做：两张表 + 一个 `emit()` 接口 + 已有模块接入 + 事件时间线页面。

## 交付物

```
migrations/0002_events.oracle.sql
migrations/0002_events.mysql.sql
internal/events/
  emit.go       Emit(ctx, Event) —— 全工程唯一的事件入口
  types.go      内置事件类型注册表
  store.go      入库 + 处置策略判定
  README.md
internal/api/events/    事件列表查询、标记已读、类型策略的读改
web/                    事件时间线页面（列表 + 过滤 + 未读数）
```

建表：`event_types`、`events`，列定义照 `09-events-notify.md` §2.3。

## 第一期要接入的事件

| event_type | 由谁发 | 默认级别 | 默认处置 |
|---|---|---|---|
| `node.online` | 任务 10 的连接管理 | info | store |
| `node.offline` | 任务 10 的离线判定 | warning | store+notify |
| `node.enrolled` | 任务 10 的注册端点 | info | store |
| `node.removed` | 任务 14 的节点删除 | warning | store |
| `auth.login_failed` | 任务 14 的登录 | warning | store |
| `system.db_unreachable` | 任务 11 的落库失败 | critical | store+notify |

**接入方式是在这些位置加一行 `events.Emit(...)`，不改它们的其他逻辑。**

## 接口签名（照这个写）

```go
package events

type Event struct {
    Type       string         // 如 "node.offline"
    Source     string         // 模块名，如 "control"
    TargetKind string         // "node" / "user" / ""
    TargetID   string
    Title      string
    Payload    map[string]any // 模板变量，见 09-events-notify.md §5.2
    DedupKey   string         // 如 "node.offline:<node_id>"，可空
    OccurredAt int64          // 0 表示用当前时间
}

// ★ Emit 永不返回错误、永不阻塞。写不进去只打日志。
// 事件系统故障绝不允许影响业务流程。
func Emit(ctx context.Context, e Event)

// 注册内置事件类型，进程启动时调用，写入 event_types 表（已存在则只补默认值不覆盖用户设置）
func RegisterType(t TypeDef)

type TypeDef struct {
    Type               string
    DisplayName        string
    DefaultSeverity    string // info / warning / critical
    DefaultDisposition string // drop / store / store+ui / store+notify
    Description        string
}
```

`Emit` 内部：查 `event_types` 的当前 `disposition`（内存缓存，类型策略变更时失效）→
`drop` 直接返回 → 其余写入 `events`。**写库走异步队列**，`Emit` 只做一次 channel 投递。

表结构见 [`../../10-schema-spec.md`](../../10-schema-spec.md) §4。
接口见 [`../../12-api-spec.md`](../../12-api-spec.md) §7。

## 第一期六种事件的 payload

★ **`payload` 里放什么，决定了第二期模板里能写什么变量。**
补齐后要同步更新 `09-events-notify.md` §5.2 的变量表。

| event_type | payload 字段 |
|---|---|
| `node.online` | `node_name` `group_name` `public_ip` `offline_seconds`（本次离线时长） |
| `node.offline` | `node_name` `group_name` `public_ip` `last_seen_at_ms` |
| `node.enrolled` | `node_name` `public_ip` `os_name` `arch` |
| `node.removed` | `node_name` `operator` |
| `auth.login_failed` | `username` `ip` `fail_count` |
| `system.db_unreachable` | `error_msg` `failed_seconds` `dropped_batches` |

## 约束

- ★ **`Emit()` 必须是非阻塞的、永不返回错误给调用方。**
  事件写不进去只能打日志，**绝不允许因为事件系统故障而让业务失败**
- ★ **`emit()` 是全工程唯一的事件入口。** 不允许模块自己往 `events` 表写
- `store+notify` 在第一期**只是入库时打个标记**，不做任何外发——渠道和路由是第二期
- **事件类型由代码定义，界面不能新增**，只能改级别和处置方式
- `payload_json` 里要放齐 `09-events-notify.md` §5.2 表里列的变量。
  **新增事件类型时同步补那张表**，否则第二期写模板的人不知道能用什么变量
- `dedup_key` 用于同类事件去重，形如 `node.offline:<node_id>`
- 事件保留 90 天，走任务 12 已有的分块清理机制，**不要另写一套清理**
- 表和列名照 `04` 的规范：小写、不加引号、≤30 字符、避开保留字、时间用 `*_ms`

## 验收

1. 六种事件都能在对应操作后正确产生，`events` 表里能查到
2. **停掉数据库，触发一次节点上线，业务正常完成，只在日志里看到事件写入失败**
   —— 这条是 `Emit()` 非阻塞的关键验证
3. 界面时间线能按类型、级别、目标节点过滤，未读数正确
4. 在界面上把 `node.online` 的处置改成 `drop`，之后该类事件不再入库
5. `09-events-notify.md` §5.2 的变量表已按实际 payload 补全
6. `scripts/lint-sql.sh` 通过；两份 DDL 对比测试通过

## 边界

**不做**通知渠道、路由规则、消息模板、Telegram、投递记录。那些是第二期。

---

# 验收记录

## 第 1 轮 · 2026-09-07 · ✅ 通过

分支 `agy/p1-20-events`，提交 `e96b643`。已合并到 main。

| 验收项 | 实测 |
|---|---|
| 测试 | ✅ `internal/events`、`internal/api/events` 通过 |
| ★ `Emit` 非阻塞、不返错 | ✅ 无返回值；store 未初始化时只 `logx.Warn` 后 return；正常路径只做一次 `Enqueue` |
| 迁移 0002 | ✅ `0002_events.{oracle,mysql}.sql` 两份齐全 |
| **真库迁移** | ✅ 对 ADB 执行 `Successfully applied migration 0002_events`，退出码 0 |
| 事件时间线页面 | ✅ `web/src/pages/Events/` |

### 流程提醒

本任务往 `main` 直接推了一次文档提交（`aa5b36a` 补 `09-events-notify.md` §5.2 变量表）。
内容正确、也是任务书要求的，本次不追究；但按 `03-rules.md` §5，
**AGY 不提交 `docs/`**，需要改文档时在 PR 说明里写清楚，由设计方改到 main。

**任务 20 关闭。**

---

## 第 2 轮 · 2026-09-07 · ❌ 不通过（1 项必修）

端到端联调时发现：**六种事件里只有 1 种真正接入了**。

```
node.online            接入点: 0   ❌
node.offline           接入点: 0   ❌
node.enrolled          接入点: 0   ❌
node.removed           接入点: 0   ❌
auth.login_failed      接入点: 0   ❌
system.db_unreachable  接入点: 2   ✅

events.Emit( 的全部调用点（events 包外）：
  internal/ingest/writer.go:223
```

实测：agent 上线、节点创建都发生了，但 `SELECT COUNT(*) FROM events` = **0**。

### ❌ F1 必须修 —— 补齐 5 个事件的接入点

任务书「第一期要接入的事件」那张表要求**在这些位置加一行 `events.Emit(...)`**：

| event_type | 接入位置 |
|---|---|
| `node.online` | `internal/control` 的连接建立 / 状态转 online 处 |
| `node.offline` | `internal/control` 的 sweeper 判离线处 |
| `node.enrolled` | `internal/control` 的 enroll 端点成功后 |
| `node.removed` | `internal/inventory` 的节点删除事务提交后 |
| `auth.login_failed` | `internal/auth` 的登录失败分支 |

`payload` 字段照任务书那张表填齐（`node_name` / `group_name` / `public_ip` /
`offline_seconds` / `last_seen_at_ms` / `username` / `ip` / `fail_count` 等），
**这决定了第二期模板里能用哪些变量**。

`dedup_key` 用 `<event_type>:<node_id>` 形式。

### 复验方式

```sh
grep -rn "events.Emit(" --include=*.go internal/ | grep -v _test   # 应有 ≥6 处
# 端到端：起 dashd + agent，上线后查库
SELECT event_type, COUNT(*) FROM events GROUP BY event_type;       # node.online 应有行
```

### 我的验收失误

第 1 轮我只验了 `Emit` 的语义（非阻塞、不返错）和迁移，**没有检查六个调用点是否存在**，
所以放过了。这类「接口实现了但没人调用」的缺陷，静态测试抓不到，必须靠端到端。
后续涉及跨模块接入的任务，验收时一律加一条「调用点计数」检查。

---

## 第 3 轮 · 待验收

## 第 3 轮 · 2026-09-07 · ✅ 通过

分支 `agy/p1-20-events`，提交 `4ae93a4`。已合并到 main：`01ffb0d`。

| 事件 | 接入点 |
|---|---|
| `node.online` | ✅ 2 处 |
| `node.offline` | ✅ 1 处 |
| `node.enrolled` | ✅ 1 处 |
| `node.removed` | ✅ 1 处 |
| `auth.login_failed` | ✅ 2 处 |
| `system.db_unreachable` | ✅ 2 处 |

`events.Emit` 调用点分布在 7 个文件：`auth/login.go`、
`control/{enroll,fallback,hello,registry}.go`、`ingest/writer.go`、`inventory/nodes.go`。

### ★ 端到端实测（上一轮就是这里露的馅）

起 dashd + agent，agent 上线后查 `/api/v1/events`：

```json
{"event_type":"node.online","severity":"info","source_module":"control",
 "title":"节点 e2e-test 上线",
 "payload":{"node_name":"e2e-test","public_ip":"127.0.0.1",
            "offline_seconds":3,"group_name":""},
 "dedup_key":"node.online:01M1XNETY18JNSD67B9Z4W9SF8"}
```

payload 字段与任务书要求的表完全对应，`dedup_key` 格式正确。
`public_ip` 是服务端从 `RemoteAddr` 记录的——符合 `11-collect-spec.md` §9.3。

**任务 20 关闭。**
