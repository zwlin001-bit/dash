# P1-10 服务端 Agent 接入与在线状态

> 第一期 · 任务 10/19 ｜ 前置：04、05 ｜ 分支：`agy/p1-10-control`
> 设计依据：[`../../04-protocol.md`](../../04-protocol.md) §1、§5、§6

## 目标

agent 能连上来、能被认出来、在线状态是准的。

## 交付物

```
internal/control/
  ws.go        /api/agent/v1/rpc WebSocket 端点
  fallback.go  /api/agent/v1/report HTTP 回退端点
  registry.go  在线连接注册表
  enroll.go    /api/agent/v1/enroll 注册端点
  hello.go     agent.hello 处理与参数下发
  README.md
```

## 端点规格

★ 端点、请求体、响应体、状态码全在 [`../../12-api-spec.md`](../../12-api-spec.md) §2，照着实现。

几个容易做错的：
- token 无效时**在 WebSocket 升级前返回 401**，不要升级成功后再用 RPC 错误码告知
- HTTP 回退的响应体里 `commands` 数组是**回退模式下唯一的下行通道**，不能只回 200
- `enroll` 是**无需认证**的端点，但要限流（同一 IP 每分钟 10 次）

## 在线状态机

```
never ──首次 hello──▶ online ──90s 无消息──▶ offline
                        ▲                       │
                        └────── 重新连上 ───────┘
```

- 用**单个 sweeper goroutine 每 10 秒扫一遍**在线表，不给每个连接开定时器
- 判离线的依据是「最后一次收到任何帧的时间」，**包括 WS ping**
- 状态变化时：更新 `nodes.conn_state` + `last_seen_at_ms`，
  推 SSE `node_state` 事件，并 `events.Emit` 一条 `node.online` / `node.offline`
- ★ **dashd 启动时把所有 `conn_state='online'` 的节点重置为 `offline`**，
  等 agent 重连再置回。否则上次崩溃残留的 online 状态会一直骗人

## 公网 IP 由服务端记录

★ agent 不上报 IP（`11-collect-spec.md` §9.3）。服务端在连接建立时：

- 从 `RemoteAddr` 取地址，写入 `node_facts.ipv4` 或 `ipv6`
- 反代后面读 `X-Forwarded-For` / `X-Real-IP`，**且只信任来自本机（127.0.0.1/::1）的反代**，
  否则任何 agent 都能伪造自己的 IP

## 约束

- **enrollment token**：一次性、15 分钟有效、可预绑定节点名与分组。用过即作废
- **长期 token 只存哈希**（`nodes.agent_token_hash`），明文只在签发那一刻返回一次。
  吊销 = 置空该列
- **`agent.hello` 的应答里下发采集参数**（`interval_fast_s` / `interval_slow_s` /
  `collect_conns` / `need_facts` 等）。**服务端是采集配置的权威**，agent 本地配置只是兜底
- `need_facts`：比对 agent 报上来的 `facts_hash` 与 `node_facts.facts_hash`，
  不一致才要求上报 facts
- **同一 node 重复连接要踢掉旧连接**，否则重连风暴会堆积僵尸连接
- **90 秒没收到任何消息（含 ping）判离线**，更新 `nodes.conn_state` 与 `last_seen_at_ms`
- **时钟偏差**：`server_ts_ms - ts_ms` 超过 60 秒时以服务端时间入库，并在节点上打标记。
  VPS 时钟跑偏很常见，不处理会导致图表出现来自未来或远古的点
- token 无效返回 `-32000`，协议版本不兼容返回 `-32001`
- 注册与吊销**写审计日志**

## 验收

1. 生成 enrollment token → agent 注册成功 → 建出 `nodes` 行 → 返回长期 token
2. **同一个 enrollment token 再用一次被拒**
3. 吊销长期 token 后，agent 收到 `-32000` 并停止重连
4. `kill -9` 掉 agent，**90 秒内**节点标记为离线
5. agent 重启后自动恢复在线
6. 同一节点开两个 agent 进程，旧连接被踢掉
7. 把 agent 机器时间调快 10 分钟，入库时间仍然正确且节点上有时钟异常标记

## 边界

**只管连接、认证、在线状态。指标落库是任务 11 的事**，这里收到 `agent.metrics` 先丢弃或打日志。

---

# 验收记录

## 第 1 轮 · 2026-09-07 · ✅ 通过

分支 `agy/p1-10-control`，提交 `d961e55`。已合并到 main。

| 验收项 | 实测 |
|---|---|
| 测试 | ✅ `internal/control` 通过（含 513 行 ws_test） |
| 90 秒判离线 | ✅ |
| 同 node 重复连接踢旧连接 | ✅ |
| 时钟偏差处理（60 秒阈值） | ✅ |
| 反代 IP（`X-Forwarded-For` / `RemoteAddr`） | ✅ |
| 启动时把残留 online 重置为 offline | ✅ |

### 待联调补验（依赖真实 agent 端到端）

kill -9 后 90 秒内标记离线、agent 重启自动恢复在线、时钟调快 10 分钟后入库时间仍正确 ——
这三条需要 agent 与服务端同时跑，等任务 11 落库打通后一起做。

**任务 10 关闭。**
