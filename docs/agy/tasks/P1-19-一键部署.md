# P1-19 setup.sh 一键部署 ▶ 里程碑：一条命令交付

> 第一期 · 任务 19/19 ｜ 前置：04、15 ｜ 分支：`agy/p1-19-setup`
> 设计依据：[`../../05-deployment.md`](../../05-deployment.md) 全文

> ⚠️ **第 1 轮已通过并合并（单域名方案）。本轮为重做，目标是双域名架构。**
>
> ★ **完整设计已就绪：[`../../14-dual-domain-deploy.md`](../../14-dual-domain-deploy.md)，
> 开工前完整读一遍，实现以它为准。** 下面这段只是摘要。

## 需求变更（待细化为正式任务书）

### 关键差异（相对第 1 轮）

| | 第 1 轮（已合并） | 本轮 |
|---|---|---|
| 域名 | 1 个 | **2 个** |
| TLS 终结 | dashd 内置 autocert | **nginx** |
| dashd 监听 | `0.0.0.0:443`（setcap） | **`127.0.0.1:8443`**，不再需要 setcap |
| 控制台暴露面 | 公网 | **仅 WireGuard 内网** |
| 依赖 | 无 | **nginx（setup.sh 负责装好配好）** |

### 三个最容易做错的地方

1. ★ **控制台的 nginx server 块必须 `listen <内网IP>:443`，不是 `listen 443`。**
   绑 `0.0.0.0` 等于把控制台重新暴露到公网，这条做错整个架构就没意义了
2. ★ **内网域名不能用 ACME HTTP-01 签证书** —— Let's Encrypt 验证不到内网 IP，
   会一直失败且原因难查。必须 DNS-01 或自签
3. ★ **公网 server 块只放行 `/api/agent/v1/`、`/dl/`、`/install.sh`，其余 `return 404`。**
   验收会实测 `curl https://<公网域名>/` 必须返回 404

### 双域名双入口

| 入口 | 域名 | 绑定 | 用途 |
|---|---|---|---|
| **管理控制台** | 内网域名 | **内网 IP**（WireGuard 网段） | 仅本人访问，走 WG 进来，攻击面最小 |
| **Agent 接入** | 互联网域名 | 公网 | agent 上报与拉取指令 |

- `setup.sh install` **要求输入两个域名**
- 两个域名分别签发证书
- **控制台入口绑内网 IP，不监听公网**——这是这套架构的安全核心
- agent 接入入口只暴露 `/api/agent/v1/*` 与 `/dl/*`，**不暴露控制台 API 和界面**

### nginx + 应用一起发布

`setup.sh install` 直接把依赖装好、配好、起好，用户不需要单独装 nginx。
nginx 负责两个 server 块（内网/公网）与 TLS 终结，dashd 监听本地回环。

### 服务端资源预算

稳态 RSS ≤ 1 GB，1–2 GB 内存的机器能跑。具体见 [`P1-21-dashd主程序装配.md`](P1-21-dashd主程序装配.md)。

---

## 目标

**用户视角只有一句话：`sudo ./setup.sh install`，只问域名。**

## 交付物

```
setup.sh    子命令：install / upgrade / uninstall / status
systemd unit + OpenRC 脚本
ACME 证书自动申请与续期（autocert）
```

## 约束

- ★ **交互只有一个必填项：域名。** 其余全部有合理默认值
- 非交互：`./setup.sh install --domain x --yes`
- **POSIX sh**，Alpine busybox ash 能跑
- **全部子命令幂等**，可重复执行
- 默认监听 **443**；`setcap cap_net_bind_service=+ep` 后**以非 root 用户 `dashd` 运行**
  （Alpine 需先 `apk add libcap`）。验收会查 `ps -o user=`
- 80 端口只做 ACME HTTP-01 挑战 + 301 跳 443
- ★ **域名写入数据库 `settings.site.domain`，不写配置文件**（P5.4）
- ★ **ADB 已关闭 mTLS，用连接串直连，不需要 wallet。**
  数据库连接信息通过 `--db-dsn` / `--db-user` / `--db-password` 或环境变量提供，
  写入 `/etc/dash/config.toml`（`0600`）
- 落文件：
  ```
  /usr/local/bin/dashd
  /etc/dash/config.toml     0600 dashd:dashd
  /var/lib/dash/            0750 dashd:dashd
  ```
- 安装流程：前置检查 → 建用户 → 落文件 → setcap → 写服务 →
  跑迁移 → 写入域名 → 建管理员 → 启动 → 等 `/healthz` 通过
- **健康检查失败要打印日志尾部**，不要静默失败
- `status` 要显示：服务状态、版本、数据库连通性、证书到期时间、在线 agent 数
- `upgrade` 健康检查失败**自动回滚到旧二进制**
- `uninstall` 默认保留 `/etc/dash` 和数据库，`--purge` 才删配置，**数据库永远不动**
- 安装完成打印：访问地址、初始管理员密码（只显示一次）、
  **需要备份的文件：`/etc/dash/master.key`（若已生成）、`/etc/dash/config.toml`**

## 验收

三个系统各跑一次，PR 里贴输出：

1. Debian / Ubuntu / Alpine 干净环境各跑一次 `install`，装完能用域名 HTTPS 访问
2. **重复执行 `install` 不报错**
3. `ps -o user= -p $(pidof dashd)` **不是 root**
4. `status` 各项都正确
5. `upgrade` 到一个故意坏掉的二进制，**自动回滚**且服务继续可用
6. `uninstall` 后数据库数据仍在
7. `busybox ash -n setup.sh` 语法检查通过

## 边界

域名在线修改（改了自动重签证书）第一期不做，设置页先只读展示。

---

# 验收记录

## 第 1 轮 · 2026-09-07 · ✅ 通过

分支 `agy/p1-19-setup`，提交 `db2f809`。已合并到 main：`ac8e9cc`。

> 本轮按**原任务书**（单域名）验收。双域名 / nginx 一起发布 / 内网绑定这三项需求
> 是验收之后提出的，由后续任务书补实现，**不影响本轮判定**。

### ★ 端到端首次跑通（本期最重要的里程碑）

19 顺带把 `dashd` 的 serve 模式实现了（原本是任务 21 的范围），因此第一次能做完整链路验证：

```
1. dashd init-db          → 建表 + 写入 site.domain + 创建管理员（随机密码）
2. dashd -config ...      → 启动，/healthz 返回 {"db":"ok","status":"ok"}
3. 登录 → 建 enrollment token → install_cmd 里带上了数据库里的域名
4. POST /api/agent/v1/enroll → 返回 node_id + 长期 agent_token
5. dash-agent --token ... → WebSocket 连上
6. 界面查节点            → conn_state=online, agents_online=1, clock_skew_ms=1
7. 查 ADB                → sample_host 16 行、sample_dim 8 行
```

真实入库数据：

```
ts=1788775811185 cpu=11.63 mem=669642752 net_total_up=11203411980 traffic_up=352224  tcp=NULL
ts=1788775806182 cpu= 6.37 mem=666746880 net_total_up=11203059756 traffic_up=67937   tcp=NULL
ts=1788775801183 cpu= 9.50 mem=666984448 net_total_up=11202991819 traffic_up=867622  tcp=NULL
ts=1788775796186 cpu= 4.60 mem=655065088 net_total_up=11202124197 traffic_up=66864   tcp=43
```

★ 这组数据同时验证了三件事：
- **5 秒间隔准确**（时间戳差 ~5000ms）
- **任务 11 的计数器增量逻辑在真实链路里正确** —— `traffic_up` 是增量不是累计值，
  无负数、无天文数字
- **slow 档只在周期到期那轮上报** —— `tcp_count` 交替出现 NULL 与真实值，
  完全符合 `08-field-map.md` §1.2 的设计

由此，任务 **10 / 11 / 12 / 16 / 17 里所有「待联调补验」的项目一并关闭**。

### ✅ setup.sh 静态检查

| 项 | 结果 |
|---|---|
| `sh -n setup.sh` 语法 | ✅ 通过（本机无 busybox，用 dash 作 POSIX 代理） |
| 只问域名 / `--domain` 非交互 | ✅ |
| 默认 443 | ✅ |
| `setcap cap_net_bind_service` | ✅ |
| 非 root 用户运行 | ✅ |
| 域名写数据库（`init-db --domain`） | ✅ 实测 `Site domain configured` |
| upgrade 回滚 | ✅ 有备份与回滚分支 |
| uninstall 不动数据库 | ✅ |
| 幂等判断 | ✅ 20 处存在性检查 |
| systemd + OpenRC | ✅ `deploy/dashd.service`、`deploy/dashd.initd` |

### 合并时由设计方修复的集成缺陷

★ **`/healthz` 被两个模块重复注册**（`internal/api/web.go` 与 `internal/ingest/module.go`），
`http.ServeMux` 直接 panic，服务起不来：

```
panic: pattern "GET /healthz" ... conflicts with pattern "GET /healthz"
```

已统一由 `internal/api` 提供（`12-api-spec.md` §9），ingest 侧不再注册。
**根因是我没在任务书里指明 `/healthz` 归谁**，两个任务各实现了一份。

另外 19 的分支基于旧 main，模块列表缺 `control`/`ingest`/`settings`，
`app.App` 也缺 `Registry`/`Ingester` 字段，合并时都已取并集补回。

### ⚠️ 遗留（不阻塞本轮，记入后续任务）

| # | 问题 |
|---|---|
| 1 | 启动日志字符串拼接错误：`http://localhost127.0.0.1:18443`，应为 `http://127.0.0.1:18443` |
| 2 | **agent 没有 enroll 能力** —— 命令行只有 `-token`，无 `-enroll`；配置里的 `enroll_token` 未被消费。按 `04-protocol.md` §6，agent 无长期 token 时应自行调 enroll 换取。本次是我手工调 API 拿 token 才跑通的。**归属任务 18** |
| 3 | `GET /api/v1/settings` 返回 `endpoint not found`，设置读取接口未挂载 |

### 未验证（需 docker / root）

三系统干净环境 install、重复 install 幂等、`ps -o user=` 非 root、
upgrade 故意坏包自动回滚、uninstall 后数据仍在。

**任务 19 关闭。**

---

## 第 2 轮（双域名重做）· 2026-09-07 · ❌ 不通过（2 项必修）

分支 `agy/p1-19-setup`，提交 `9d9bea8`。

### ✅ 已通过 —— 不要再动这些

| 验收项 | 实测 |
|---|---|
| ★ 控制台绑内网 IP | ✅ `listen ${_i_ip}:443 ssl;` —— **不是裸 `listen 443`**，这条最关键的做对了 |
| ★ 公网 `location / { return 404; }` | ✅ 控制台 API 与前端不暴露到公网 |
| dashd 只绑回环 | ✅ 默认 `127.0.0.1:8080`，不再需要 setcap |
| 三个交互项 | ✅ 控制台域名、agent 域名、内网 IP（可自动探测 WireGuard/LAN） |
| 非交互参数 | ✅ `--console-domain` / `--agent-domain` / `--internal-ip` |
| 两个域名入库 | ✅ `site.domain`（agent）+ `site.console_domain` |
| 依赖自动安装 | ✅ nginx / openssl / libcap / curl，Alpine 与 Debian 系分别处理 |
| WebSocket 与 SSE 转发头 | ✅ `Upgrade` / `Connection` / `proxy_read_timeout 300s` |
| `sh -n setup.sh` | ✅ POSIX 语法通过 |

### ❌ F1 必须修 —— `/install.sh` 不在公网白名单，一键装机直接 404

公网 server 块只放行了三处：

```nginx
location /healthz          { ... }
location /api/agent/v1/    { ... }
location /dl/              { ... }
location /                 { return 404; }   # ★ /install.sh 落进这里
```

而服务端生成的一键装机命令是（`internal/inventory/enroll.go:112`）：

```
curl -fsSL https://<agent_domain>/install.sh | sh -s -- --enroll <token>
```

★ **这条命令会 404，装机流程从第一步就断了。**
（dashd 本身是提供 `/install.sh` 的，我实测过返回完整脚本；是 nginx 把它挡了。）

**修法**：公网 server 块加一条，放在 `location /` 之前。

```nginx
location = /install.sh {
    proxy_pass http://${_listen};
    proxy_set_header Host              $host;
    proxy_set_header X-Real-IP         $remote_addr;
    proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto https;
}
```

用 `location =` 精确匹配，不要用前缀匹配。

### ❌ F2 必须修 —— 公网域名用自签证书，agent 无法建立 TLS

`setup.sh` 里 **`certbot` / `acme.sh` 出现 0 次**，两个域名都是 `openssl req -x509` 自签：

```
153/158: openssl req -x509 ...   → /etc/dash/certs/admin.{crt,key}
169:     openssl req -x509 ...   → /etc/dash/certs/agent.{crt,key}
```

内网域名自签是对的（`14-dual-domain-deploy.md` §4 就是这么设计的）。
但 **公网域名自签会让 agent 的 TLS 校验失败** —— agent 要么连不上，
要么被迫开 `insecure_skip_verify`，两者都不能接受。

**修法**（照 `14-dual-domain-deploy.md` §4）：

| 域名 | 方式 |
|---|---|
| **agent 域名（公网）** | **ACME HTTP-01**。公网可达，走标准流程拿受信任证书。
`location /.well-known/acme-challenge/` 已经有了，把签发接上即可 |
| 控制台域名（内网） | 保持自签（或提供 DNS-01 选项）。★ **不要试图给内网域名走 HTTP-01**，Let's Encrypt 验证不到内网 IP，会一直失败且原因难查 |

要求：
- `setup.sh` 增加 `--acme-email <mail>`；给了就对 agent 域名走 ACME，没给则自签并**明确警告
  「agent 将无法校验证书，仅供测试」**
- 签发失败要给出可读原因（80 端口被占、域名未解析到本机、限流），**不要静默回落自签**
- 装好续期（certbot timer / cron），并在 `setup.sh status` 里显示两个证书的到期时间

### 复验方式

```sh
sh -n setup.sh
# F1：公网块必须能命中 /install.sh
grep -n "location = /install.sh" setup.sh          # 必须有
# F2：必须存在 ACME 分支
grep -cE "certbot|acme" setup.sh                    # 必须 > 0
grep -n "acme-email" setup.sh                       # 必须有参数

# 有环境的话实测（这两条是这轮的核心）
curl -I https://<agent_domain>/install.sh           # 必须 200，不是 404
curl -I https://<agent_domain>/                     # 必须 404
curl -I https://<console_domain>/  --resolve ...    # 内网可达，公网不可达
openssl s_client -connect <agent_domain>:443 </dev/null 2>&1 | grep -i "verify"   # 必须 verify ok
```

### 未验证（需 root + 真实域名）

三系统实跑 install、nginx 配置语法、证书签发、`ps -o user=` 非 root、
upgrade 回滚、uninstall 保留数据库。

---

## 第 3 轮 · 待验收
